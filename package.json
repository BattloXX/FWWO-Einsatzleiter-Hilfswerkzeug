{
  "name": "einsatzleiter-cloud-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "Tailwind build pipeline fuer das einsatzleiter.cloud-Frontend. Reine Dev-Dependency; das Ergebnis (app/static/css/app.css) wird ins Repo committed, sodass Deploy ohne Node funktioniert.",
  "scripts": {
    "build": "tailwindcss -i ./app/static/css/tailwind.input.css -o ./app/static/css/app.css --minify",
    "dev": "tailwindcss -i ./app/static/css/tailwind.input.css -o ./app/static/css/app.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0",
    "@tailwindcss/forms": "^0.5.7",
    "@tailwindcss/container-queries": "^0.1.1"
  }
}
